using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Froggie : EnemyStateMachine
{
    void Start()
    {
        // animator = GetComponent<Animator>();
        // SetState(new FollowState(this));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
